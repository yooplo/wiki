name: Django CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.12'  # Use the version you are developing in

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt  # Ensure you have this file in your repo

    - name: Run migrations
      run: |
        python manage.py migrate  # Initializes your SQLite database

    - name: Run tests
      run: |
        python manage.py test  # Run your Django tests

    - name: Collect static files
      run: |
        python manage.py collectstatic --noinput  # Collect static files for deployment
        
